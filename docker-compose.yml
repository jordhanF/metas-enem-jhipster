services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: metasEnem
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: metasEnem
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
